# 参考录制样例（冻结）

本目录下的 `data.jsonl`、`data.mov`、`calibration.json`、`metadata.json` 为 **Spectacular Rec 风格的历史参考数据**，用于文档与格式对照。

- **请勿**为对齐应用实现而覆盖或批量修改这些文件；新录制会话由 App 写入用户选定目录。
- 应用侧双机位实现说明见仓库根目录文档：`docs/Spectacular样例对齐差距说明.md`。
